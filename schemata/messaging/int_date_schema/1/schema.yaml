name: fixed_decimal
doc: Metadata of the execution signal specific to the Active Watchdog strategy.
namespace: messaging
type: record
fields:

  - name: date
    description: Estimated profit
    type: {"type": "int", "name": "Date", "logicalType": "date"}
